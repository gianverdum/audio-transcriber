# =============================================================================
# AUDIO TRANSCRIBER ENVIRONMENT CONFIGURATION
# =============================================================================
# Copy this file to .env and configure your credentials

# =============================================================================
# OPENAI API CONFIGURATION (REQUIRED)
# =============================================================================
# Get your key at: https://platform.openai.com/account/api-keys
OPENAI_API_KEY=your_api_key_here

# =============================================================================
# API SERVER CONFIGURATION
# =============================================================================

# Server host (default: 127.0.0.1)
SERVER_HOST=127.0.0.1

# Server port (default: 8000)
SERVER_PORT=8001

# Docker exposed port (port mapped from host to container)
DOCKER_PORT=8000

# Number of server workers (default: 1)
# SERVER_WORKERS=1

# Auto-reload mode for development (true/false)
# SERVER_RELOAD=false

# API title
# API_TITLE=Audio Transcriber API

# API version
# API_VERSION=1.0.0

# API description
# API_DESCRIPTION=Complete API for audio transcription using OpenAI Whisper

# =============================================================================
# OPTIONAL CONFIGURATIONS
# =============================================================================

# Default folder for audio files
# DEFAULT_AUDIO_FOLDER=./audios

# Default folder for output files
# DEFAULT_OUTPUT_FOLDER=./output

# Log level (DEBUG, INFO, WARNING, ERROR)
# LOG_LEVEL=INFO

# Timeout for API requests (seconds)
# API_TIMEOUT=30

# Delay between requests to avoid rate limiting (seconds)
# API_DELAY=0.5

# Maximum file size in MB
# MAX_FILE_SIZE_MB=25

# =============================================================================
# DEVELOPMENT CONFIGURATION
# =============================================================================

# Debug mode (true/false)
# DEBUG=false

# Save logs to file (true/false)
# SAVE_LOGS=false

# =============================================================================
# INSTRUCTIONS
# =============================================================================
# 1. Copy this file: cp .env.example .env
# 2. Edit the .env file with your configurations
# 3. Never commit the .env file to git (it's already in .gitignore)
# 4. Always keep this .env.example file updated for other developers
